# Codecov configuration for REvoDesign
# https://docs.codecov.com/docs/codecovyml-reference

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
  require_head: yes
  show_carryforward_flags: false

github_checks:
  annotations: true
