Bootstrap: docker-daemon
From: revodesign-revocompute-runner-placer-rfdiffusion:latest

%post
    echo "PLACER/RFdiffusion runtime containerised"

%runscript
    exec bash /app/revocompute/run.sh "$@"
